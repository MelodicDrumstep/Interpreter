proc main() {
  int n;
  int i;
  int s;
  n = read_int();
  i = 0;
  s = 0;
  while (i < n) do {
    s = s + read_int();
    i = i + 1;
    continue;
    write_int(108)
  };
  write_int(s);
  write_char(10)
}
